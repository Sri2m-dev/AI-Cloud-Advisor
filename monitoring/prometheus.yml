global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']
  # Add your app metrics endpoint here
  # - job_name: 'ai-cloud-advisor'
  #   static_configs:
  #     - targets: ['host.docker.internal:8000']
